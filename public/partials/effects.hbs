<!-- Script to Activate the Effects in Blog.html -->
<script>
    /*jslint undef: true */

    var Effects = (function () {
        'use strict';

        var toggleNavOnScroll = function (elementScrolled, elementHidden) {
            var hideNav = function () {
                elementHidden.addClass('hide-preserving');
            };

            var showNav = function () {
                elementHidden.removeClass('hide-preserving');
            };

            $(document).on('scroll', function () {
                var positionOnPage = elementScrolled[0].getBoundingClientRect().top;
                if (positionOnPage < 0) {
                    showNav();
                }
            });

            // End of scroll
            $(document).on('scroll', function () {
                var positionOnPage = elementScrolled[0].getBoundingClientRect().top;
                if (positionOnPage >= 0) {
                    showNav();
                } else {
                    hideNav();
                }
            }, 2000);
        };

        return {
            toggleNavOnScroll: toggleNavOnScroll
        };
    })();
</script>